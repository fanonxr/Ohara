#!/usr/bin/env sh
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)

if [ -x "$SCRIPT_DIR/.venv/bin/ohara-review" ]; then
  exec "$SCRIPT_DIR/.venv/bin/ohara-review" "$@"
fi

exec python -m ohara.cli.review "$@"
